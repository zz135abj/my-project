# 第2课：变量与数据类型

## 📖 学习目标
- 理解变量的概念和作用
- 掌握Python的基本数据类型
- 学会变量的命名规则
- 掌握类型转换的方法

## 📚 理论知识

### 什么是变量？
变量是程序中用来存储数据的容器。你可以把变量想象成一个贴了标签的盒子，里面可以存放不同类型的数据。

### 变量的命名规则
1. **只能包含**：字母、数字、下划线
2. **不能以数字开头**
3. **区分大小写**：`name`和`Name`是不同的变量
4. **不能使用Python关键字**：如`if`、`for`、`while`等
5. **建议使用有意义的名称**：提高代码可读性

### 命名建议
- 使用小写字母和下划线：`student_name`
- 避免使用单字母变量（除循环计数器外）
- 使用驼峰命名法：`studentName`（也可）

## 🔢 Python基本数据类型

### 1. 整数类型 (int)
表示没有小数部分的数字，包括正数、负数和零。

```python
age = 20          # 正整数
temperature = -5  # 负整数
count = 0         # 零
```

### 2. 浮点数类型 (float)
表示带有小数部分的数字。

```python
height = 175.5    # 身高
weight = 65.0     # 体重
pi = 3.14159      # 圆周率
```

### 3. 字符串类型 (str)
表示文本数据，用单引号或双引号包围。

```python
name = "张三"      # 使用双引号
city = '北京'      # 使用单引号
message = "Hello, Python!"  # 英文
```

### 4. 布尔类型 (bool)
表示逻辑值，只有两个值：`True`和`False`。

```python
is_student = True
is_graduated = False
has_license = True
```

## 🔄 类型转换

### 常用类型转换函数
- `int()`：转换为整数
- `float()`：转换为浮点数
- `str()`：转换为字符串
- `bool()`：转换为布尔值

```python
# 转换为整数
num_str = "123"
num_int = int(num_str)  # 123

# 转换为浮点数
num_int = 45
num_float = float(num_int)  # 45.0

# 转换为字符串
age = 20
age_str = str(age)  # "20"

# 转换为布尔值
num_zero = 0
bool_zero = bool(num_zero)  # False
num_positive = 5
bool_positive = bool(num_positive)  # True
```

## 💻 代码示例

### 变量的创建和使用
```python
# 创建变量
name = "李明"
age = 20
height = 175.5
is_student = True

# 使用变量
print(f"姓名：{name}")
print(f"年龄：{age}")
print(f"身高：{height}cm")
print(f"是学生：{is_student}")
```

### 查看变量类型
```python
name = "张三"
age = 20
height = 175.5
is_student = True

print(f"name的类型：{type(name)}")      # <class 'str'>
print(f"age的类型：{type(age)}")        # <class 'int'>
print(f"height的类型：{type(height)}")  # <class 'float'>
print(f"is_student的类型：{type(is_student)}")  # <class 'bool'>
```

### 类型转换示例
```python
# 字符串转数字
str_num = "100"
num = int(str_num)
print(f"字符串'{str_num}'转换为整数：{num}")

# 数字转字符串
age = 25
age_str = str(age)
print(f"整数{age}转换为字符串：'{age_str}'")

# 布尔值转换
print(f"bool(0) = {bool(0)}")      # False
print(f"bool(1) = {bool(1)}")      # True
print(f"bool('') = {bool('')}")    # False
print(f"bool('hello') = {bool('hello')}")  # True
```

## ✏️ 练习题目

### 练习1：变量创建
创建变量存储你的个人信息并输出。

```python
# 请在这里创建变量
name = "你的名字"
age = 你的年龄
height = 你的身高
hobby = "你的爱好"

# 输出信息
print(f"姓名：{name}")
print(f"年龄：{age}")
print(f"身高：{height}cm")
print(f"爱好：{hobby}")
```

### 练习2：类型转换练习
完成下列类型转换操作。

```python
# 字符串转整数
str_age = "22"
age_int = int(str_age)
print(f"年龄：{age_int}，类型：{type(age_int)}")

# 整数转字符串
score = 95
score_str = str(score)
print(f"分数：'{score_str}'，类型：{type(score_str)}")

# 浮点数转整数
price = 19.99
price_int = int(price)
print(f"价格：{price_int}元，类型：{type(price_int)}")
```

### 练习3：计算BMI
编写程序计算BMI指数。

```python
# 输入数据
weight = 65.5  # 体重（kg）
height = 1.75  # 身高（m）

# 计算BMI
bmi = weight / (height ** 2)

# 输出结果
print(f"体重：{weight}kg")
print(f"身高：{height}m")
print(f"BMI指数：{bmi:.2f}")
```

### 练习4：学生信息管理
创建多个变量存储学生信息，并进行类型转换。

```python
# 学生信息
student_name = "王五"
student_id = "2023001"
chinese_score = "85"
math_score = "92"
english_score = "78"

# 转换分数为数字
chinese_score_num = int(chinese_score)
math_score_num = int(math_score)
english_score_num = int(english_score)

# 计算总分和平均分
total_score = chinese_score_num + math_score_num + english_score_num
average_score = total_score / 3

# 输出结果
print(f"学生姓名：{student_name}")
print(f"学号：{student_id}")
print(f"总分：{total_score}")
print(f"平均分：{average_score:.1f}")
```

## 🎯 挑战任务

编写一个简单的个人信息调查程序：
1. 收集用户的姓名、年龄、身高、体重、职业等信息
2. 将所有信息存储在变量中
3. 计算BMI指数
4. 以格式化的方式输出所有信息

```python
# 个人信息调查程序
print("=== 个人信息调查 ===")

# 创建变量存储信息
name = "张小明"
age = 25
height = 1.78
weight = 70.0
occupation = "软件工程师"

# 计算BMI
bmi = weight / (height ** 2)

# 输出结果
print(f"\n=== 调查结果 ===")
print(f"姓名：{name}")
print(f"年龄：{age}岁")
print(f"身高：{height}m")
print(f"体重：{weight}kg")
print(f"职业：{occupation}")
print(f"BMI指数：{bmi:.1f}")

# BMI健康提示
if bmi < 18.5:
    print("健康提示：体重偏轻，建议适当增加营养")
elif bmi < 24:
    print("健康提示：体重正常，请继续保持")
else:
    print("健康提示：体重偏重，建议适当运动")
```

## 📝 学习笔记

请记录以下内容：
1. 变量的命名规则和最佳实践
2. Python的基本数据类型及其特点
3. 类型转换的注意事项
4. 在什么情况下需要类型转换

## 🔗 扩展阅读

- [Python数据类型详解](https://www.runoob.com/python3/python3-data-type.html)
- [Python变量命名规范](https://pep8.org/#naming-conventions)
- [Python类型转换指南](https://docs.python.org/3/library/stdtypes.html#type-conversion)

---

**下节课预告**：运算符与表达式 - 学习如何使用各种运算符进行数学计算和逻辑判断。
